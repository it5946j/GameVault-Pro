import ComingSoon from "../components/Common/ComingSoon";

export default function Dashboard() {
  return (
    <ComingSoon
      title="Dashboard — Coming in Phase 3"
      description="Profile, inventory, orders, wishlist, notifications, uploads, sales, and revenue will live here once authentication is wired up."
    />
  );
}
